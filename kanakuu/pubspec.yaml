name: kanakuu
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.4.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Firebase
  firebase_core: ^2.24.2
  firebase_auth: ^4.15.3
  cloud_firestore: ^4.13.6
  uuid: ^4.1.0
  # Authentication & Security
  local_auth: ^2.1.6
  shared_preferences: ^2.2.2

  # UI & State Management
  cupertino_icons: ^1.0.6
  lottie: ^2.7.0  # <-- ✅ Add this

  # Utilities
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true